# 检查PCI总线的硬件

# 显卡
lspci | grep -i nvidia 

# 网卡
lspci | grep -i net

ip add # 查看
lsmod | grep nvidia # 查看模块是否加载


# 安装驱动
官网下载，直接运行脚本
sudo su # 切换root

# 禁用开源驱动 Nouveau
echo "blacklist nouveau" | sudo tee /etc/modprobe.d/blacklist-nouveau.conf
echo "options nouveau modeset=0" | sudo tee -a /etc/modprobe.d/blacklist-nouveau.conf
sudo update-initramfs -u
sudo reboot

lsmod | grep nouveau # 验证是否禁止使用

# 安装驱动
更新系统
sudo apt update && sudo apt upgrade -y
sudo apt install build-essential linux-headers-$(uname -r) -y
### 驱动模块必须和当前正在运行的内核版本匹配
linux-headers-$(uname -r) = 当前内核对应的内核头文件

安装依赖
sudo apt install build-essential libssl-dev pkg-config dkms libglvnd-dev -y


sudo systemctl stop gdm3 # 停止图形界面服务
 Ctrl + Alt + F3 (或 F1-F6) 进入一个虚拟终端（TTY）

 chmod +x NVIDIA-Linux-*.run
 sudo su
 ./NVIDIA-Linux-*.run # 运行
 安装（5060要使用 “Open Kernel Modules”（开源内核模块））

 sudo systemctl start gdm3  # 或 lightdm # 启动图形服务
 sudo reboot

 nvidia-smi # 查看